# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/
# Development and testing files
values-dev.yaml
values-test.yaml
*.test
*.test.yaml
# CI/CD files (if any get copied by mistake)
.github/
# Documentation that shouldn't be in the package
CONTRIBUTING.md
docs/
examples/
# Build artifacts
*.tgz
*.tar.gz
# Logs
*.log