{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "karo.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "karo.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with include "karo.annotations" . }}
    {{- . | nindent 4 }}
  {{- end }}
{{- end }}
